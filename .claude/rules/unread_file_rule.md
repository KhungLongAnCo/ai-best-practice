IMPORTANT

- DO NOT read, reference, or infer values from the `.env` file.
- Treat all `.env` values as unknown.
- NEVER include secrets in code or responses.

Environment Variables:

- Any new environment variable MUST be added to `.env.example` only.
- `.env.example` entries must include:
  - Variable name
  - Short description comment
  - Example placeholder value (non-secret)
